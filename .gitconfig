[user]
	name = Andrew Tretyakov
	email = 42178850+0xAndoroid@users.noreply.github.com
	signingkey = ~/.ssh/github_ed25519.pub
[credential]
	helper = osxkeychain
[core]
	editor = nvim
	hooksPath = ~/.git-hooks
	excludesfile = ~/.dotfiles/.gitignore_global
[commit]
	gpgsign = true
[gpg]
	format = ssh
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[pull]
	rebase = true
	autostash = true
[alias]
	pushall = "!f() { git push \"$@\" || exit 1; repo=$(git rev-parse --show-toplevel); [ \"$repo\" = \"$HOME/.dotfiles\" ] || exit 0; for b in a16z homeserver; do git show-ref --verify --quiet refs/heads/$b && git push origin $b --force-with-lease; done; :; }; f"
[init]
	defaultBranch = main
[rebase]
  autostash = true
[merge]
	autostash = true
