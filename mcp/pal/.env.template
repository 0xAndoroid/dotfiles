# PAL MCP Server Configuration

# Model configuration
DEFAULT_MODEL=gemini-3-pro-preview
DEFAULT_THINKING_MODE_THINKDEEP=max
OPENAI_ALLOWED_MODELS=gpt-5.2-codex,gpt-5.2-pro
GOOGLE_ALLOWED_MODELS=gemini-3-pro-preview
XAI_ALLOWED_MODELS=grok-4

# Session settings
CONVERSATION_TIMEOUT_HOURS=24
MAX_CONVERSATION_TURNS=40

# Logging
LOG_LEVEL=INFO
LOG_MAX_SIZE=10MB

# Disabled tools (comma-separated)
DISABLED_TOOLS=analyze,refactor,testgen,secaudit,docgen,tracer,planner,consensus,precommit,clink,challenge,apilookup,version,debug

# Misc
PAL_MCP_FORCE_ENV_OVERRIDE=false
COMPOSE_PROJECT_NAME=pal-mcp
TZ=UTC
